<%# 1. Update the Flash %>
<%= turbo_stream.update "flash_messages", partial: "layouts/shared/flash" %>

<%# 2. Re-render the row (to update timestamp) %>
<%= turbo_stream.replace dom_id(@invitation), partial: "invitations/invitation", locals: { invitation: @invitation } %>